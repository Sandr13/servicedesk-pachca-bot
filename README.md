# servicedesk-pachca-bot
Этот проект — серверless-обработчик (обычно размещённый в Яндекс Функции), который обеспечивает двустороннюю связь между Pachca и Яндекс.Tracker.

Файл index.py реализует:

создание задач в Трекере из сообщений в Пачке

обратную синхронизацию: комментарии из Трекера → отправляются в тред задачи в Пачке

комментарии из Пачки → добавляются в комментарии Трекера

поиск задач по кастомным полям

обработку кнопок

защиту вебхуком и HMAC-подписью

создание тредов для сообщений

идемпотентность, чтобы избежать циклов

замену @логинов Яндекс-пользователей на displayName пользователей Pachca

Вместе это образует устойчивую двустороннюю интеграцию для ServiceDesk.

✨ Основные функции
1. 📨 Получение вебхуков от Пачки

Обработчик принимает JSON из Pachca:

обычные сообщения (type=message)

кнопки

сообщения в тредах (entity_type=thread)

служебные сообщения

события callback

данные пользователя и т. д.

Тело вебхука проверяется по HMAC-подписи (pachca-signature) для защиты от посторонних вызовов.

2. 🏷 Создание задач в Яндекс.Трекере

Если сообщение создаёт новую заявку, функция:

Формирует структуру задачи

Добавляет тег "Создана в Пачке"

Создаёт задачу в очереди (SERVICEDESK)

Записывает ключ задачи в custom-поле

Создаёт ответное сообщение в Пачке → отправляет кнопки и ссылку на задачу

Для обновления полей задачи используется:

update_tracker_issue(issue_key, extra_fields)

3. 🔄 Обработка комментариев из Tracker → Pachca

Событие issueCommentEvent обрабатывается мгновенно, без проверки подписи, так как приходит напрямую от Яндекс.Tracker.

Функция:

Ищет кастомное поле pachcaMessageId

Определяет к какому сообщению/треду относится задача

Формирует сообщение вида:

🔔 Добавлен комментарий:
Автор: Иван Иванов
Текст комментария


Отправляет его в тред или в чат Пачки

Использует идемпотентность, чтобы избежать дублирования

4. 🔃 Обратная синхронизация: комментарии из Pachca → Tracker

Если пользователь что-то пишет в треде в Пачке, это означает комментарий к задаче.

Функция:

Идентифицирует, что сообщение находится внутри треда

Находит связанную задачу Трекера

Добавляет комментарий через:

add_tracker_comment(issue_key, text)


Игнорирует сообщения, созданные самим ботом, чтобы не создавать цикл

5. 🔎 Поиск задач по кастомному полю «PachcaUserId»

Фильтрует задачи:

очередь = 29

тег = "Создана в Пачке"

66aa43bfc941f16869268d41--pachcaUserId == user_id из webhook

фильтрация по открытым/закрытым статусам

Используется в сценариях назначения исполнителей, проверки прав пользователя, работы с кнопками и т.д.

6. 🧵 Работа с тредами Pachca
Создание треда:
create_thread_for_message(message_id)

Отправка сообщения в тред:
send_threaded_message(thread_entity_id=..., text="...")


Возможна отправка:

напрямую в тред

в ответ конкретному сообщению

просто в чат

7. 🛡 Идемпотентность (anti-loop)

Проблема:
комментарий в Трекере → отправляется в тред Пачки → тред Пачки создаёт комментарий → новый комментарий → вновь тред → цикл.

Решение:
простейшее in-memory кеширование ID комментариев:

_processed_comments_cache
already_processed_comment(id)
mark_comment_processed(id)


В проде рекомендуется заменить на Redis, DynamoDB, Yandex Database или S3+TTL.

8. 👤 Замена упоминаний @username

Если в комментарии Трекера встречается что-то вроде:

@ivan.petrov


Скрипт:

пытается найти пользователя Pachca по email / query

заменяет упоминание на ник пользователя в Пачке, чтобы он был правильно упомянут в Пачке

Используется:

find_pachca_user_by_query(query)

🔧 Структура кода

Основные блоки:

Раздел	Описание
Конфигурация	Чтение env-переменных, токены, домены, API URL
Утилиты Pachca	отправка сообщений, создание тредов
Утилиты Tracker	обновление задач, создание комментариев
Антиспам	кэш обработанных comment_id
Поиск пользователей Pachca	API /users?query=
Обработчики событий	handle_issue_comment_event() и handler()
Основная логика	Разбор webhook, HMAC, маршрутизация события
🔐 Необходимые переменные окружения (.env)
PACHKA_API_TOKEN=
WEBHOOK_SECRET=

TRACKER_API_TOKEN=
TRACKER_ORG_ID=
TRACKER_CREATE_URL=

BOT_USER_ID=

Y360_OAUTH=
Y360_ORG_ID=

PACHCA_EMAIL_DOMAIN=bnovo.ru
TRACKER_QUEUE=SERVICEDESK

🚀 Деплой

Обычно используется Яндекс Функция:

runtime: Python 3.12/3.10

timeout: 5–10 сек

memory: 256–1024 MB

Зависимости:

requests

📚 Логика работы handler()

Основной handler выполняет:

Принимает HTTP webhook

Пытается быстро распознать issueCommentEvent

Проверяет подпись HMAC

Фильтрует сообщения бота

Определяет тип события:

Тип события	Действие
message в чате	создаёт задачу в трекере
message в треде	добавляет комментарий в задачу
кнопка	меняет статус, вызывает команды
issueCommentEvent	отправляет комментарий в тред Пачки
другое	игнорируется

Всё логирует

Возвращает корректный HTTP статус
